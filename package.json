{
  "name": "23127364-hw04-ai-automation",
  "version": "1.0.0",
  "private": true,
  "description": "HW04 automation testing deliverables for EShop SUT.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "mcp:playwright": "playwright-mcp --browser chromium --headless --output-dir mcp-output"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.79",
    "@playwright/test": "^1.54.0",
    "csv-parse": "^5.6.0"
  }
}
